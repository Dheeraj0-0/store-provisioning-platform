# store-provisioning-platform
A multi-tenant store provisioning system built using:  - React (Dashboard) - FastAPI (Orchestrator API) - Helm (Deployment templating) - Kubernetes (Namespace-per-store isolation) - WordPress + MySQL (Store engine)
